model Transaction {
  id        String   @id @default(cuid())
  assetName String
  sector    String
  category  String
  amount    Float
  currency  String   @default("USD")
  risk      Int // Skala 0-5
  rationale String?
  createdAt DateTime @default(now())
}
